Web server configuration
========================

While you can run the LocalWiki software using Django's built-in development
server, for a public-facing setup you're definitely better off using it with
Apache and mod_wsgi.  In this section we'll walk you through setting that up.

